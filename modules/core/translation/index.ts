/**
 * Translation module barrel.
 *
 * v1: only the interface is exported.
 * Future: GoogleTranslateAdapter will implement TranslationProvider.
 */

export type { TranslationProvider } from "./types";
